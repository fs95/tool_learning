*		查找当前变量或单词 'n': 下一个 'N': 上一个
f{char}		查找字符 ';':下一个 ',': 上一个
daw		delete a word
<C-a> <C-x> 	分别对数字进行加减操作
g~, gu, gU	反转大小写,小写,大写
<, >, =		减少缩进,增加缩进,自动缩进
<C-h> 		退格键
<C-[>		切换到普通模式
<C-o>		切换到插入-普通模式
zz 		重绘屏幕,当前行居中
yt,		复制到','字符之前
yf,		复制到','包括','字符
<C-r>0		读取寄存器 0, 寄存器0保存复制的内容
<C-r><C-p>0	粘贴寄存器 0
<C-r>=		=寄存器是表达式寄存器,可用来计算
r, R		字符替换, 替换模式(字符替换),相当于Insert按键
gr, gR		字符替换, 替换模式(占位宽度替换)
可视模式下按o	切换可视选择的两头活动端
w, e, b, ge	下个单词的开头,下个单词的结尾,上个单词的开头,上个单词的结尾
gv 		重复选择上次可视模式的文本
<C-o>		跳转到上一个位置
@:		重复执行Ex命令
:move, :copy	移动, 复制
:normal		执行普通模式命令
:%{command} 	%代表整个文件范围
:%normal i//	注释掉整个文件

